services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: cronwatch
      POSTGRES_PASSWORD: cronwatch
      POSTGRES_DB: cronwatch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
